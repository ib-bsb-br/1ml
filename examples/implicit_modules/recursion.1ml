import "prelude"
import "../../basis/list.1ml"

type SHOW = {
    type t
    show : t ~> text
}

combine = rec f => 
  fun [A : SHOW] [B : SHOW] (a : list A.t) (b : list B.t) => 
    case {nil = nil, x::xs = case {nil = nil, y::ys = (x, y) :: (f xs ys)} b} a
